This directory is intentionally left minimal.

Most fixtures for tests are generated deterministically at runtime (see `tests/conftest.py`)
to keep the repository lightweight and avoid committing large binary assets.

